<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Reducimos el tamaño a 64dp para asegurar que entre en la "Safe Zone" (zona segura) -->
    <!-- de los iconos adaptativos y no se vea demasiado ampliado o cortado -->
    <item
        android:width="64dp"
        android:height="64dp"
        android:gravity="center"
        android:drawable="@drawable/logo_vexo_app" />
</layer-list>
